<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kayıt Ol</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #4caf50, #81c784); /* Yeni renkler */
            margin: 0;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #333;
        }

        .container {
            background: #fff;
            padding: 2rem;
            border-radius: 20px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 380px;
            text-align: center;
            position: relative;
        }

        .logo {
            width: 80px;
            height: auto;
            position: absolute;
            top: -40px;
            left: calc(50% - 40px);
            border-radius: 50%;
            border: 3px solid #4caf50; /* Logo çerçevesi */
            background: #fff;
            padding: 5px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .container h1 {
            margin-top: 2rem;
            margin-bottom: 1.5rem;
            font-size: 1.8rem;
            color: #388e3c; /* Yeni renk */
        }

        .container form {
            display: flex;
            flex-direction: column;
            align-items: stretch;
        }

        .container label {
            text-align: left;
            margin-bottom: 0.5rem;
            font-weight: 600;
            font-size: 1.1rem;
            color: #388e3c; /* Yeni renk */
        }

        .container input {
            padding: 0.8rem;
            border: 1px solid #ddd;
            border-radius: 12px;
            margin-bottom: 1.2rem;
            font-size: 1rem;
            transition: all 0.3s;
        }

            .container input:focus {
                border-color: #388e3c; /* Yeni renk */
                outline: none;
                box-shadow: 0 0 8px rgba(56, 142, 60, 0.3);
            }

        .container button {
            padding: 0.8rem;
            background: #388e3c; /* Yeni renk */
            color: #fff;
            border: none;
            border-radius: 12px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: background 0.3s, transform 0.3s;
        }

            .container button:hover {
                background: #66bb6a; /* Yeni hover rengi */
                transform: translateY(-3px);
            }

        .container p {
            margin-top: 1rem;
            font-size: 0.9rem;
        }

            .container p a {
                color: #388e3c; /* Yeni renk */
                text-decoration: none;
                font-weight: bold;
            }

                .container p a:hover {
                    text-decoration: underline;
                }

        /* Hata mesajları stili */
        .error-message {
            color: #d32f2f;
            font-size: 0.9rem;
            margin-bottom: 1rem;
        }

        .error-summary {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 1.5rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <a href="~/images/logo.png">
            <img src="~/images/logo.png" alt="Logo" class="logo" />
        </a>

        <h1>Hesap Oluştur</h1>

        <!-- Hata mesajları: ValidationSummary kullanılarak dinamik hata mesajları -->
        @Html.ValidationSummary(true, "", new { @class = "error-summary" })

        <form method="post" asp-action="Register_Action" asp-controller="Admin">
            <!-- Kullanıcı adı -->
            <label for="username">Kullanıcı Adı:</label>
            <input type="text" id="username" name="username" placeholder="Kullanıcı adınızı girin" required>

            <!-- Şifre -->
            <label for="password">Şifre:</label>
            <input type="password" id="password" name="password" placeholder="Şifrenizi girin" required>

            <!-- Şifre Tekrar -->
            <label for="confirmPassword">Şifre Tekrar:</label>
            <input type="password" id="confirmPassword" name="confirmPassword" placeholder="Şifrenizi tekrar girin" required>

            <!-- Kayıt ol butonu -->
            <button type="submit">Kayıt Ol</button>

            <p>Zaten hesabınız var mı? <a asp-controller="Admin" asp-action="Login">Giriş Yap</a></p>
        </form>
    </div>
</body>
</html>
